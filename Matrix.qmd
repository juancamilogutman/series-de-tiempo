---
title: "Wooldridge"
format: pdf
lang: es
editor: visual
---

## Matrices

```{r}
library(Matrix)
A <- matrix( c(2,-4,-1,5,7,0), nrow=2)
B <- matrix( c(2,1,0,3,-1,5), nrow=2)
```

```{r}
print(A)
```

```{r}
print(B)
```

```{r}
A*B #Esto multiplica elemento por elemento
```

```{r}
C <- t(B) #Transpongo
print(C)
```

```{r}
D <- A %*% C #Multiplicación de matrices posta
```

```{r}
solve(D) #Inversa
```

```{r}
det(D)
```

```{r}
a <- c(1,3)
b <- c(3,9)
E <- cbind(a,b)
det(E)
```
